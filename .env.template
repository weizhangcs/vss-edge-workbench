# Visify Story Studio - Environment Variables Template
# ----------------------------------------------------
# This file is a template for the .env file.
# The init_setup.sh script will use this file to generate the final .env
# Do not fill in secrets here directly.
# ----------------------------------------------------

# --- Public Endpoint Configuration ---
# IMPORTANT: This will be prompted for during setup. e.g., http://your-server-ip
PUBLIC_ENDPOINT=

# --- Django Core Settings ---
DJANGO_SECRET_KEY=
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=

# --- PostgreSQL Database Settings ---
POSTGRES_DB=visify_ssw_db
POSTGRES_USER=visify_ssw_user
POSTGRES_PASSWORD=

# --- Celery/Redis Settings ---
CELERY_BROKER_URL=redis://redis:6379/0

# --- Public Facing URLs (Derived from PUBLIC_ENDPOINT) ---
# These are automatically derived from PUBLIC_ENDPOINT
LABEL_STUDIO_PUBLIC_URL=${PUBLIC_ENDPOINT}:8081
SUBEDITOR_PUBLIC_URL=${PUBLIC_ENDPOINT}:3000
LOCAL_MEDIA_URL_BASE=${PUBLIC_ENDPOINT}:9999
NUXT_PUBLIC_VSS_WORKBENCH_URL=${PUBLIC_ENDPOINT}:8000

# --- CORS Settings ---
# This is automatically derived from SUBEDITOR_PUBLIC_URL
CORS_ALLOWED_ORIGINS=${SUBEDITOR_PUBLIC_URL}

# --- Storage Backend ---
STORAGE_BACKEND=local

# --- AWS S3 Settings (Required even if STORAGE_BACKEND is 'local') ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=
AWS_S3_CUSTOM_DOMAIN=

# --- Label Studio Settings ---
# This token is manually generated from the Label Studio UI (Account & Settings page).
# It is required for Django to communicate with the Label Studio API.
LABEL_STUDIO_ACCESS_TOKEN=
LABEL_STUDIO_INTERNAL_URL=http://label-studio:8080

# --- Initial Setup Settings ---
DJANGO_SUPERUSER_EMAIL=
# IMPORTANT: The password will be prompted for securely when running the init script.
DJANGO_SUPERUSER_PASSWORD=